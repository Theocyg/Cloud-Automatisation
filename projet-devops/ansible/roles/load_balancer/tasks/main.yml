---
- name: 
  apt:
    name: nginx
    state: present
    update_cache: yes

- name: Configurer le Load Balancing
  template:
    src: nginx.conf.j2
    dest: /etc/nginx/nginx.conf
  notify: Restart Nginx

- name: Démarrer Nginx
  service:
    name: nginx
    state: started
    enabled: yes
